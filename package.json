{
  "name": "testeweb_qa",
  "version": "1.0.0",
  "description": "Automação de testes para o Blog do Agi",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "test:ci": "cypress run --browser chrome"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": "cypress/support/step_definitions/[filepath].steps.js"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^24.0.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.8",
    "cypress": "^13.17.0",
    "esbuild": "^0.28.1"
  }
}
